# This public showcase does not require real credentials.
# Keep production secrets only in a private deployment environment.
TELEGRAM_BOT_TOKEN=replace_with_your_token
GAME_API_CLIENT_ID=replace_with_client_id
GAME_API_CLIENT_SECRET=replace_with_client_secret
